services:
  postgres:
    image: postgres:16-alpine
    container_name: clientix-postgres
    environment:
      POSTGRES_USER: clientix
      POSTGRES_PASSWORD: clientix_dev_pass
      POSTGRES_DB: clientix
    ports: ["5432:5432"]
    volumes: [postgres_data:/var/lib/postgresql/data]

  redis:
    image: redis:7-alpine
    container_name: clientix-redis
    ports: ["6379:6379"]

  rabbitmq:
    image: rabbitmq:3.13-management-alpine
    container_name: clientix-rabbitmq
    environment:
      RABBITMQ_DEFAULT_USER: clientix
      RABBITMQ_DEFAULT_PASS: clientix_dev_pass
    ports: ["5672:5672", "15672:15672"]

volumes:
  postgres_data: