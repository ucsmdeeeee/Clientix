clientix-assist.ru {
    # API запросы ? WebApi
    handle /api/* {
        reverse_proxy webapi:5050
    }

    # Webhook ЮKassa ? BotGateway (как было)
    handle /payments/* {
        reverse_proxy botgateway:5000
    }

    # Всё остальное — пока на BotGateway (потом заменим на лендинг)
    handle {
        reverse_proxy botgateway:5000
    }
}
